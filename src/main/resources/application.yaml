spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ams
    username: postgres
    password: focus0data
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
token:
  signing:
    key: 46327457FGH842YH

